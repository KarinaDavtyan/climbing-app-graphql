# import Route from "../Route/type.graphql"
# import User from "../User/type.graphql"

interface Post {
  _id: ID!
  user: User!
}

type SportPost implements Post {
  _id: ID!
  user: User!
  route: Route!

}

type LeisurePost implements Post {
  _id: ID!
  user: User!
}
